// client/src/views/Editor/DropEditor/config/types.ts
// 掉落编辑器类型定义

import type { FieldMetaItem, CategoryConfig } from '../../config/sharedTypes';

/** 掉落字段元数据项 - 使用统一字段定义 */
export type DropMetaItem = FieldMetaItem;

/** 掉落向导分类配置 - 使用统一分类定义 */
export type { CategoryConfig };
