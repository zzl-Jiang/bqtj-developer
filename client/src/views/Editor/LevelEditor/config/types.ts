// client/src/views/Editor/LevelEditor/config/types.ts
// 关卡编辑器类型定义

import type { FieldMetaItem, CategoryConfig } from '../../config/sharedTypes';

/** 关卡字段元数据项 - 使用统一字段定义 */
export type LevelMetaItem = FieldMetaItem;

/** 关卡向导分类配置 - 使用统一分类定义 */
export type { CategoryConfig };
